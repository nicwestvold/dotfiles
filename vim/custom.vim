" all custom settings should be added here.

" set packpath^=~/.vim
" packadd minpac
" call minpac#init()
" minpac must have {'type': 'opt'} so that it can be loaded with `packadd`.
" call minpac#add('k-takata/minpac', {'type': 'opt'})
" call minpac#add('wakatime/vim-wakatime')
